apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
  namespace: team-burnham
spec:
  replicas: 3
  selector:
    matchLabels:
      component: web
  template:
    metadata:
      labels:
        component: web
    spec:
      containers:
        - name: client
          image: ubosquet/client:1.0
          ports:
            - containerPort: 3000
